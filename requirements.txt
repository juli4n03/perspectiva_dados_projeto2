# Dependências principais do projeto (v1 + v2).
# Instale com: pip install -r requirements.txt
#
# torch e tabicl ficam de fora de propósito — a instalação certa depende de
# você ter GPU (CUDA) ou não. Ver seção "7 (opcional) — TabICL com GPU" no
# README para o comando correto de instalação.

# Dados e manipulação
pandas==2.0
numpy==1.24
pyarrow==14.0        # leitura/escrita de modelos/catalogo.parquet

# Coleta (v1 — scraper)
requests==2.31
beautifulsoup4==4.12

# Modelagem
scikit-learn==1.3
joblib==1.3

# Explicabilidade
shap==0.44

# Visualização
matplotlib==3.7

# App
streamlit==1.30

# Notebooks
jupyter==1.0
nbformat==5.9

# Download automático dos dados do Google Drive (baixar_dados.py)
gdown==5.1
